<Window x:Class="WindowsMagnifier.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="放大镜设置"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#18181E"
        BorderBrush="#8B1E3C"
        BorderThickness="1">

    <Window.Resources>

        <!-- ── 颜色定义 ── -->
        <SolidColorBrush x:Key="BgWindow"      Color="#18181E"/>
        <SolidColorBrush x:Key="BgCard"        Color="#212129"/>
        <SolidColorBrush x:Key="BgInput"       Color="#2A2A35"/>
        <SolidColorBrush x:Key="BgInputHover"  Color="#32323F"/>
        <SolidColorBrush x:Key="AccentWine"    Color="#8B1E3C"/>
        <SolidColorBrush x:Key="AccentHover"   Color="#A8253F"/>
        <SolidColorBrush x:Key="AccentPressed" Color="#6B1630"/>
        <SolidColorBrush x:Key="AccentMuted"   Color="#3D1020"/>
        <SolidColorBrush x:Key="BorderColor"   Color="#353545"/>
        <SolidColorBrush x:Key="TextPrimary"   Color="#F0EFF5"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8888A0"/>
        <SolidColorBrush x:Key="TextMuted"     Color="#555568"/>

        <!-- ── Section 标题样式 ── -->
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="#8B1E3C"/>
            <Setter Property="FontSize"    Value="11"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="Margin"      Value="0,0,0,8"/>
        </Style>

        <!-- ── 普通文本样式 ── -->
        <Style x:Key="BodyText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ── 数值标签 ── -->
        <Style x:Key="ValueLabel" TargetType="TextBlock">
            <Setter Property="Foreground"     Value="{StaticResource AccentWine}"/>
            <Setter Property="FontSize"       Value="13"/>
            <Setter Property="FontWeight"     Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinWidth"       Value="36"/>
            <Setter Property="TextAlignment"  Value="Right"/>
        </Style>

        <!-- ── 卡片面板样式 ── -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background"    Value="{StaticResource BgCard}"/>
            <Setter Property="BorderBrush"   Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius"  Value="8"/>
            <Setter Property="Padding"       Value="16,14"/>
            <Setter Property="Margin"        Value="0,0,0,10"/>
        </Style>

        <!-- ── ComboBox ── -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1" CornerRadius="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="24"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="0" Margin="10,0,0,0"
                                      VerticalAlignment="Center"/>
                    <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                          Fill="{StaticResource TextSecondary}"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </ControlTemplate>

        <Style TargetType="ComboBox">
            <Setter Property="Background"           Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground"           Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"          Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness"      Value="1"/>
            <Setter Property="Height"               Value="32"/>
            <Setter Property="Width"                Value="70"/>
            <Setter Property="FontSize"             Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Template="{StaticResource ComboBoxToggleButton}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay,
                                                      RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter Margin="10,0,28,0"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide" Placement="Bottom">
                                <Border Background="{StaticResource BgCard}"
                                        BorderBrush="{StaticResource AccentWine}"
                                        BorderThickness="1" CornerRadius="6"
                                        MaxHeight="200" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentWine}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground"  Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background"  Value="Transparent"/>
            <Setter Property="Padding"     Value="10,6"/>
            <Setter Property="FontSize"    Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentMuted}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentMuted}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentWine}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Slider ── -->
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="{StaticResource AccentWine}"
                                                        CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="{StaticResource BgInput}"
                                                        CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="16" Height="16"
                                                         Fill="{StaticResource AccentWine}"
                                                         Stroke="#C0305A" StrokeThickness="1.5">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="#8B1E3C" BlurRadius="6"
                                                                          ShadowDepth="0" Opacity="0.6"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Cursor" Value="Hand"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── CheckBox ── -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="Margin"     Value="0,0,0,8"/>
            <Setter Property="Cursor"     Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="CheckBorder"
                                    Width="18" Height="18" CornerRadius="4"
                                    Background="{StaticResource BgInput}"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1.5"
                                    Margin="0,0,10,0">
                                <Path x:Name="CheckMark"
                                      Data="M 2 8 L 6 13 L 14 4"
                                      Stroke="{StaticResource TextPrimary}"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background"
                                        Value="{StaticResource AccentWine}"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush"
                                        Value="{StaticResource AccentWine}"/>
                                <Setter TargetName="CheckMark" Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush"
                                        Value="{StaticResource AccentWine}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── 主按钮（确定） ── -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background"       Value="{StaticResource AccentWine}"/>
            <Setter Property="Foreground"       Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="FontSize"         Value="13"/>
            <Setter Property="FontWeight"       Value="SemiBold"/>
            <Setter Property="Height"           Value="36"/>
            <Setter Property="Width"            Value="90"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{StaticResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{StaticResource AccentPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── 次按钮（取消） ── -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Height"          Value="36"/>
            <Setter Property="Width"           Value="90"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="BorderBrush"
                                        Value="{StaticResource AccentWine}"/>
                                <Setter Property="Foreground"
                                        Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{StaticResource AccentMuted}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- ── 主布局 ── -->
    <Grid Background="{StaticResource BgWindow}">
        <StackPanel Margin="20,0,20,20">

            <!-- 标题栏 -->
            <Border BorderBrush="{StaticResource AccentWine}"
                    BorderThickness="0,0,0,1" Margin="-20,0,-20,20" Padding="20,18,20,16">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 酒红竖线装饰 -->
                        <Border Width="3" Height="20" CornerRadius="2"
                                Background="{StaticResource AccentWine}" Margin="0,0,12,0"/>
                        <TextBlock Text="放大镜设置"
                                   Foreground="{StaticResource TextPrimary}"
                                   FontSize="16" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 卡片：放大倍数 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="放大倍数" Style="{StaticResource SectionLabel}"/>
                    <StackPanel x:Name="DisplayMagnificationPanel"/>
                </StackPanel>
            </Border>

            <!-- 卡片：窗口高度 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="窗口高度" Style="{StaticResource SectionLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="HeightSlider"
                                Minimum="100" Maximum="600"
                                TickFrequency="10"
                                IsSnapToTickEnabled="True"
                                VerticalAlignment="Center"
                                ValueChanged="HeightSlider_ValueChanged"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal"
                                    Margin="14,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="HeightValue" Text="300"
                                       Style="{StaticResource ValueLabel}"/>
                            <TextBlock Text=" px" Foreground="{StaticResource TextMuted}"
                                       FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 卡片：跟随模式 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="跟随模式" Style="{StaticResource SectionLabel}"/>
                    <CheckBox x:Name="FollowMouseCheckBox"
                              Content="跟随鼠标指针"
                              Checked="FollowMode_Changed"
                              Unchecked="FollowMode_Changed"/>
                    <CheckBox x:Name="FollowKeyboardInputCheckBox"
                              Content="跟随键盘输入"
                              Margin="0,0,0,0"
                              Checked="FollowMode_Changed"
                              Unchecked="FollowMode_Changed"/>
                </StackPanel>
            </Border>

            <!-- 卡片：启动选项 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="启动选项" Style="{StaticResource SectionLabel}"/>
                    <CheckBox x:Name="StartMinimizedCheckBox"
                              Content="启动后最小化"
                              Margin="0,0,0,0"
                              Checked="StartMinimized_Changed"
                              Unchecked="StartMinimized_Changed"/>
                </StackPanel>
            </Border>

            <!-- 卡片：全屏检测 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="全屏检测" Style="{StaticResource SectionLabel}"/>
                    <CheckBox x:Name="HideOnFullScreenCheckBox"
                              Content="全屏应用时自动隐藏放大镜"
                              Margin="0,0,0,0"
                              Checked="HideOnFullScreen_Changed"
                              Unchecked="HideOnFullScreen_Changed"/>
                </StackPanel>
            </Border>

            <!-- 按钮行 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                <Button Content="取消" Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0" Click="CancelButton_Click"/>
                <Button Content="确定" Style="{StaticResource PrimaryButton}"
                        Click="OkButton_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
